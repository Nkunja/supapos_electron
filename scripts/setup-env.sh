#!/bin/bash
# Setup script for Electron build environment

# Set Electron mirror for faster downloads (optional)
export ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/

echo "Environment variables set:"
echo "ELECTRON_MIRROR=$ELECTRON_MIRROR"
echo ""
echo "You can now run build commands normally."

